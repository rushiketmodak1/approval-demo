name: Prod Deployment

on:
  workflow_dispatch:

jobs:
  deploy-prod:
    runs-on: ubuntu-latest

    environment:
      name: rushi

    steps:
      - name: Deploy
        run: echo "Deploying to production"
